# Database configuration for dump script (source database - typically production)
SOURCE_DB_HOST=localhost
SOURCE_DB_PORT=5432
SOURCE_DB_NAME=spotifystatistics
SOURCE_DB_USER=postgres
SOURCE_DB_PASSWORD=your_production_password

# Database configuration for restore script (target database - typically local)
TARGET_DB_HOST=localhost
TARGET_DB_PORT=5432
TARGET_DB_NAME=spotifystatistics
TARGET_DB_USER=postgres
TARGET_DB_PASSWORD=postgres

# Output directory for data dumps (relative to scripts directory)
OUTPUT_DIR=./data-dump

# User to preserve during anonymization (won't be anonymized)
PRESERVE_USER=gavinhow